# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files — never commit real secrets.
# NOTE: .env was previously tracked in this repo's git history with live
# database credentials. Removing it here stops future commits, but the
# credentials already in history should be rotated regardless — see README.
.env
.env.*
!.env.example
backend/.env
backend/.env.*
!backend/.env.example
