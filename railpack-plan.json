{
  "$schema": "https://railpack.io/schema.json",
  "build": {
    "cmd": "npm install --omit=dev && npm run build && cd backend && npm install --omit=dev && cd .."
  },
  "start": {
    "cmd": "bash start.sh"
  }
}
